<div class="container">
 <div class="row">
  <div class= "col-md-3">

<% if @book.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
   <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
     <% @book.errors.full_messages.each do |message| %>
     <li><%= message %></li>
	 <% end %>
  </div>
 <% end %>

  <h2>Editing Book</h2>

  <%= form_for @book do |f| %>

  <div class="field">
    <br>
    <%= f.label :title, "Title" %>
    <br>
    <%= f.text_field :title, class: "col-md-9 title" %>
  </div>
    <br>
  <div class="field">
    <br>
    <%= f.label :body, "Opinion" %>
    <br>
    <%= f.text_area :body, value: @book.body, class: "col-md-9 book_body" %>
  </div>
    <br>
  <div class="actions">
    <%= f.submit "Update Book", class: "btn btn-primary col-md-3" %>
  </div>


	<%= link_to "Show", book_path(@book.id), class: "show_#{@book.id}" %>
	<%= link_to "Back", books_path, class: "back" %>

  <% end %>

    </div>
   </div>
  </div>