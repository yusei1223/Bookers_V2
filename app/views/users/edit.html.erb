<p id="notice"></p>
<div class="container">
 <div class="row">
  <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">


<% if @user.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
   <h2><%= pluralize(@user.errors.count, "error") %> prohibited this book from being saved:</h2>
     <% @user.errors.full_messages.each do |message| %>
     <li><%= message %></li>
	 <% end %>
  </div>
 <% end %>

　<h1>User info</h1>
　<%= form_with model: @user, local:true do |f| %>

  <p>name</p>
  <%= f.text_field :name,class: "col-sm-10" %>

  <p>image</p>
  <%= f.attachment_field :profile_image,class: "col-sm-10" %>

  <p>Introduction</p>
  <%= f.text_area :introduction,class:"col-sm-10" %>
  <br>
  <nav>
  <%= f.submit 'Update User',class: "col-xs-12 btn btn-primary"%>
  </nav>
 <% end %>

  </div>
 </div>
</div>
